
&НаКлиенте
Процедура ЗакрытьФорму(Команда)
	ЭтаФорма.Закрыть();
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если не Параметры.Свойство("СсылкаНаДокумент") тогда
		Отказ=Истина;
		Возврат
	КонецЕсли;
	Ссылка=Параметры.СсылкаНаДокумент;
	Данные=РеквизитФормыВЗначение("Объект");
	ТабличныейДокумент.Вывести(Данные.Сформировать(Ссылка));
КонецПроцедуры
