
&НаКлиенте
Процедура ЗадачиИнвентаризацииВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
СтандартнаяОбработка=Ложь;
МассивЗадач=новый Массив;
для Каждого Строка из ВыбраннаяСтрока цикл
	МассивЗадач.Добавить(Элементы.ЗадачиИнвентаризации.ДанныеСтроки(Строка).Ссылка);
КонецЦикла;
ОповеститьОВыборе(МассивЗадач);
КонецПроцедуры
